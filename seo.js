/* Set canonical, social URLs and absolute schema IDs to the deployed origin without guessing a domain. */
(function(){const origin=location.origin;if(!/^https?:$/.test(location.protocol))return;const path=location.pathname.replace(/index\.html$/,'');const url=origin+path;const canonical=document.createElement('link');canonical.rel='canonical';canonical.href=url;document.head.append(canonical);for(const [property,value] of [['og:url',url],['og:image',origin+'/aarvi-logo.jpg']]){const el=document.querySelector('meta[property="'+property+'"]');if(el)el.content=value;else{const m=document.createElement('meta');m.setAttribute('property',property);m.content=value;document.head.append(m)}}document.querySelectorAll('script[type="application/ld+json"]').forEach(el=>{try{const data=JSON.parse(el.textContent);const fix=node=>{if(!node||typeof node!=='object')return;if(Array.isArray(node)){node.forEach(fix);return}if(node['@type']==='WebSite')node.url=origin+'/';if(node['@type']==='Organization'){node.url=origin+'/';if(node.logo==='/aarvi-logo.jpg')node.logo=origin+node.logo}if(node['@type']==='Service')node.url=url;Object.values(node).forEach(fix)};fix(data);el.textContent=JSON.stringify(data)}catch(e){/* retain source markup */}})})();
